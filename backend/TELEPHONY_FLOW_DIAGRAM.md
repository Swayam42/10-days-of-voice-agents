# 📞 Telephony Flow Diagram

## How Your Phone Call Reaches the AI Agent

```
┌─────────────────────────────────────────────────────────────────────┐
│                     TELEPHONY FLOW DIAGRAM                          │
└─────────────────────────────────────────────────────────────────────┘

Step 1: YOU MAKE A CALL
┌──────────────┐
│  Your Phone  │ ─────► Dial: +1-XXX-XXX-XXXX
│   📱         │        (Your LiveKit Number)
└──────────────┘
       │
       │ 📞 Cellular Network
       ▼

Step 2: LIVEKIT RECEIVES THE CALL
┌─────────────────────────────┐
│   LiveKit Cloud SIP Trunk   │ ─────► Checks: Inbound Dispatch Rules
│   (Phone Infrastructure)     │        Matches your phone number
└─────────────────────────────┘
       │
       │ Creates room: "fraud-call-abc123"
       ▼

Step 3: AGENT DISPATCH
┌──────────────────────────────┐
│  Agent Dispatch Rule Engine  │ ─────► Room: "fraud-call-*"
│  Checks: fraud-call-*        │        Matches pattern!
└──────────────────────────────┘
       │
       │ Looks for agent: "fraud-alert-agent"
       ▼

Step 4: YOUR AGENT JOINS
┌─────────────────────────────────────────────┐
│  Your Local Computer                        │
│  ┌──────────────────────────────────────┐  │
│  │  Terminal: python src/agent.py dev   │  │
│  │  Agent Name: "fraud-alert-agent"     │  │ ─────► Agent sees:
│  │  Status: ✓ Connected                 │  │        - Room: fraud-call-abc123
│  └──────────────────────────────────────┘  │        - Source: Phone (Telephony)
└─────────────────────────────────────────────┘
       │
       │ Loads fraud case from database
       ▼

Step 5: VOICE PIPELINE STARTS
┌────────────────────────────────────────────────┐
│           Voice Processing Pipeline            │
├────────────────────────────────────────────────┤
│  1. Your Speech (Phone Audio)                  │
│     ↓                                          │
│  2. Deepgram STT (Speech → Text)              │
│     "My name is Amit Patel"                    │
│     ↓                                          │
│  3. Google Gemini LLM (Text → Response)        │
│     "Got it Amit, what is your mother's..."    │
│     ↓                                          │
│  4. Murf AI TTS (Text → Speech)               │
│     "Got it Amit..." (Audio)                   │
│     ↓                                          │
│  5. Back to Your Phone                         │
└────────────────────────────────────────────────┘
       │
       │ Continues conversation loop
       ▼

Step 6: DATABASE INTERACTION
┌──────────────────────────────┐
│   SQLite Database (Local)    │
│   ┌──────────────────────┐  │
│   │  Fraud Cases Table   │  │
│   │  - Load case         │  │ ─────► Agent queries:
│   │  - Verify answer     │  │        - Get case by username
│   │  - Update status     │  │        - Verify security answer
│   └──────────────────────┘  │        - Update case status
└──────────────────────────────┘
       │
       │ Case updated: confirmed_safe/fraud
       ▼

Step 7: CALL ENDS
┌──────────────────────┐
│  Agent Says Goodbye  │ ─────► "Thank you for your time..."
│  Updates Database    │        Database: ✓ Status updated
│  Disconnects         │        Room: Closed
└──────────────────────┘
       │
       │ Your phone call ends
       ▼
   [CALL COMPLETE]


═══════════════════════════════════════════════════════════════════════

COMPARISON: Browser Call vs Phone Call

┌─────────────────────────────────────────────────────────────────────┐
│                         BROWSER CALL                                 │
└─────────────────────────────────────────────────────────────────────┘

Your Browser ──WebRTC──► LiveKit Cloud ──► Agent (fraud-alert-agent)
                                              │
Room: "voice-agent-room"                      └──► Database
Audio: 48kHz (HD)
Connection: Direct WebRTC

┌─────────────────────────────────────────────────────────────────────┐
│                          PHONE CALL                                  │
└─────────────────────────────────────────────────────────────────────┘

Your Phone ──Cellular──► SIP Trunk ──► LiveKit ──► Agent (fraud-alert-agent)
                                                      │
Room: "fraud-call-{callId}"                           └──► Database
Audio: 16kHz (Phone Quality)
Connection: SIP + WebRTC


═══════════════════════════════════════════════════════════════════════

KEY CONFIGURATION POINTS

1. Phone Number Setup:
   ┌──────────────────────────────────────┐
   │ LiveKit Dashboard > SIP > Numbers    │
   │ Your Number: +1-XXX-XXX-XXXX         │
   └──────────────────────────────────────┘

2. Inbound Dispatch Rule:
   ┌──────────────────────────────────────────────┐
   │ Name: fraud-alert-inbound                    │
   │ Trigger: Inbound Call                        │
   │ Phone: [Your purchased number]               │
   │ Room: fraud-call-{callId}                    │
   │ Identity: caller-{callId}                    │
   └──────────────────────────────────────────────┘

3. Agent Dispatch Rule:
   ┌──────────────────────────────────────────────┐
   │ Name: fraud-agent-dispatcher                 │
   │ Room Pattern: fraud-call-*                   │
   │ Agent Name: fraud-alert-agent                │
   └──────────────────────────────────────────────┘

4. Your Agent Code:
   ┌──────────────────────────────────────────────┐
   │ cli.run_app(WorkerOptions(                   │
   │     agent_name="fraud-alert-agent"           │
   │ ))                                           │
   └──────────────────────────────────────────────┘

   ⚠️ MUST MATCH: agent_name in code = Agent Name in dispatch rule


═══════════════════════════════════════════════════════════════════════

DEBUGGING FLOW

When something goes wrong, trace through this:

1. ❓ Did the call connect?
   → Check: LiveKit Dashboard > SIP > Call Logs

2. ❓ Was a room created?
   → Check: LiveKit Dashboard > Rooms
   → Should see: fraud-call-XXXXX

3. ❓ Did the agent join?
   → Check: Your terminal running agent
   → Should see: "Agent connected and ready!"

4. ❓ Is audio working?
   → Check: .env.local has DEEPGRAM_API_KEY, MURF_API_KEY

5. ❓ Is agent responding?
   → Check: Terminal logs for errors
   → Check: Google Gemini API key valid


═══════════════════════════════════════════════════════════════════════

SUCCESS INDICATORS ✅

Terminal shows:
```

======================================================================
Starting Bharat Secure Bank Fraud Alert Agent...
Call Source: Phone (Telephony) ← ✅ Detected as phone call!
Room Name: fraud-call-abc123 ← ✅ Correct room pattern!
======================================================================
Initializing voice pipeline...
Connecting to room...
Agent connected and ready! ← ✅ Agent joined successfully!
======================================================================

```

LiveKit Dashboard shows:
- Active room: fraud-call-XXXXX
- Participant: caller-XXXXX (your phone)
- Participant: fraud-alert-agent (AI)

Your phone:
- You hear: "Namaste! This is Rajesh Kumar..."
- Clear audio, no static
- Natural conversation flow


═══════════════════════════════════════════════════════════════════════

That's the complete flow! 📞 → 🤖 → 💾

Call your LiveKit number and watch the magic happen! ✨
```
